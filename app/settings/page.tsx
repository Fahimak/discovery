import HiveSettingsPage from "views/HiveSettings/HiveSettings";
import UploadVideo from "views/Upload/UploadVideo";

export default async function HiveSettings() {
  return (
    <main className="">
      <HiveSettingsPage />
    </main>
  );
}
